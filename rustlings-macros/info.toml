format_version = 1
welcome_message = """
REBUILD THE CHECKER
You have finished Rustlings. Now restore the Rustlings engine itself.
Read exercises/README.md first and complete the one-time preparation.
Every mission has a contract, a real source repair, and three levels of hints.
Your earlier repairs remain in later builds. Use n to advance, l for the map,
h for a hint, r to retry, and x to reset. Resetting can relock later missions.
"""
final_message = """
Checker restored. Your repairs now reconstruct the original Rustlings engine.
Run the final export and self-host checks in exercises/README.md.
Use the architecture record to explain how you would author another subject.
"""

[[exercises]]
name = "catalog_paths"
dir = "01_catalog"
test = true
strict_clippy = true
input_files = ["runner.rs", "grader.rs", "host.rs", "process.rs", "course.rs", "exercises.lock", "upstream.txt", "missions.tsv", "missions.json", "probes.txt", "audit.sh"]
hint = "1. Contract: Build an exercise path for both a root-level file and a directory. Preserve the exercises/ prefix and .rs suffix.\n2. Read the surrounding implementation and its callers in src/info_file.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "catalog_defaults"
dir = "01_catalog"
test = true
strict_clippy = true
hint = "1. Contract: Missing test metadata must enable tests. Missing strict_clippy metadata must remain false.\n2. Read the surrounding implementation and its callers in src/info_file.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "catalog_loading"
dir = "01_catalog"
test = true
strict_clippy = true
hint = "1. Contract: Prefer a local community catalog. Fall back to the embedded catalog only for NotFound; reject malformed or empty local catalogs.\n2. Read the surrounding implementation and its callers in src/info_file.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "solution_paths"
dir = "01_catalog"
test = true
strict_clippy = true
hint = "1. Contract: Produce the matching solutions/ path without losing the optional directory or file extension.\n2. Read the surrounding implementation and its callers in src/exercise.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "manifest_boundaries"
dir = "01_catalog"
test = true
strict_clippy = true
hint = "1. Contract: Locate only the contents of bin = [...]. Report missing delimiters; offsets are UTF-8 byte offsets.\n2. Read the surrounding implementation and its callers in src/cargo_toml.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "manifest_targets"
dir = "01_catalog"
test = true
strict_clippy = true
hint = "1. Contract: Register each exercise and only solutions that exist. Respect path prefixes and preserve catalog order.\n2. Read the surrounding implementation and its callers in src/cargo_toml.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "manifest_update"
dir = "01_catalog"
test = true
strict_clippy = true
hint = "1. Contract: Replace the bin-list contents while preserving all manifest text before and after the list.\n2. Read the surrounding implementation and its callers in src/cargo_toml.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "cargo_metadata"
dir = "02_checker"
test = true
strict_clippy = true
hint = "1. Contract: Ask Cargo for its target_directory, check process success, and parse the JSON response with useful error context.\n2. Read the surrounding implementation and its callers in src/cmd.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "cargo_arguments"
dir = "02_checker"
test = true
strict_clippy = true
hint = "1. Contract: Construct the Cargo subcommand with the requested binary and optional color. Preserve the debug development-manifest override.\n2. Read the surrounding implementation and its callers in src/cmd.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "process_status"
dir = "02_checker"
test = true
strict_clippy = true
hint = "1. Contract: Return the actual child exit status in both captured and discarded-output modes. Preserve merged output and the 30-second deadline.\n2. Read the surrounding implementation and its callers in src/cmd.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "binary_location"
dir = "02_checker"
test = true
strict_clippy = true
hint = "1. Contract: Run the compiled debug binary from Cargo target_directory, using the exercise directory as its working directory.\n2. Read the surrounding implementation and its callers in src/cmd.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "command_tail"
dir = "02_checker"
test = true
strict_clippy = true
hint = "1. Contract: Append the extra arguments in order and return the builder for chaining.\n2. Read the surrounding implementation and its callers in src/cmd.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "runtime_failure"
dir = "02_checker"
test = true
strict_clippy = true
hint = "1. Contract: Keep runtime failure visible in the collected output and return failure even when compilation succeeded.\n2. Read the surrounding implementation and its callers in src/exercise.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "checker_pipeline"
dir = "02_checker"
test = true
strict_clippy = true
hint = "1. Contract: Restore the build, optional tests, Clippy, and runtime sequence. Each failed gate must prevent a completion result.\n2. Read the surrounding implementation and its callers in src/exercise.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "exercise_entry"
dir = "02_checker"
test = true
strict_clippy = true
hint = "1. Contract: Delegate through the common pipeline with normal Clippy policy and the exercise name.\n2. Read the surrounding implementation and its callers in src/exercise.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "solution_entry"
dir = "02_checker"
test = true
strict_clippy = true
hint = "1. Contract: Check the _sol binary through the same pipeline, forcing strict Clippy for reference solutions.\n2. Read the surrounding implementation and its callers in src/exercise.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "idempotent_directory"
dir = "03_distribution"
test = true
strict_clippy = true
hint = "1. Contract: Accept an already existing directory; propagate other creation failures with the path in their context.\n2. Read the surrounding implementation and its callers in src/embedded.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "chapter_readme"
dir = "03_distribution"
test = true
strict_clippy = true
hint = "1. Contract: Create a chapter directory and write its embedded README.md there.\n2. Read the surrounding implementation and its callers in src/embedded.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "materialize_exercises"
dir = "03_distribution"
test = true
strict_clippy = true
hint = "1. Contract: Create the exercise tree, chapter READMEs, exercises, and input fixtures from the embedded catalog.\n2. Read the surrounding implementation and its callers in src/embedded.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "restore_exercise"
dir = "03_distribution"
test = true
strict_clippy = true
hint = "1. Contract: Restore exactly one embedded exercise at a caller-validated index. Propagate filesystem write failures with context.\n2. Read the surrounding implementation and its callers in src/embedded.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "reveal_solution"
dir = "03_distribution"
test = true
strict_clippy = true
hint = "1. Contract: Write the selected embedded solution under its chapter and name, and return that same path.\n2. Read the surrounding implementation and its callers in src/embedded.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "initialize_workspace"
dir = "03_distribution"
test = true
strict_clippy = true
hint = "1. Contract: Complete one missing initialization stage. Preserve existing-directory checks, workspace detection, catalog materialization, and the generated manifest.\n2. Read the surrounding implementation and its callers in src/init.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "load_progress"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Reconstruct completed exercises by name from the saved state. Count each completed exercise once; tolerate an unreadable or incomplete state.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "pending_count"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Compute pending exercises from the cached completed count.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "persist_progress"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Write the current exercise and completed names using the existing file format. Rewind and truncate so an older longer save cannot leave stale names.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "select_index"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Reject indices at or beyond the catalog length, avoid redundant writes, and persist valid changes.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "select_name"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Resolve the first exact exercise name, report unknown names, and persist the selected index.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "completion_count"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Make repeated status updates idempotent. Completing increments the count; reopening decrements it.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "reopen_exercise"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Persist only when changing a completed exercise back to pending.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "reset_source"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Restore embedded official exercises; use the existing git-stash path for community exercises. Propagate command failures.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "reset_current"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Mark the selected exercise pending before restoring its source.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "reset_by_index"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Validate the index, persist pending status, restore the corresponding file, and return its name.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "next_pending"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Search after the current exercise first, then wrap to the beginning. Translate suffix-local positions back to catalog indices.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "solution_visibility"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Preserve debug-build behavior; materialize official solutions and only expose existing community solutions.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "parallel_check_all"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Restore one stage of the parallel check-all coordinator. Keep the worker limit, result collection, status counts, and earliest pending result consistent.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "save_check_all"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Run the aggregate checker and persist its updated progress before returning.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "advance_progress"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Complete the current exercise, choose the next pending item, and run the final regression check before declaring the course finished.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "finish_message"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Render the configured completion message and progress through the original terminal output path.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "single_run_result"
dir = "04_progress"
test = true
strict_clippy = true
hint = "1. Contract: Restore the single-exercise command, including pending status on failure and advancing after successful validation.\n2. Read the surrounding implementation and its callers in src/run.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "terminal_budget"
dir = "05_terminal"
test = true
strict_clippy = true
hint = "1. Contract: Initialize a bounded terminal writer with zero consumed width and the requested maximum.\n2. Read the surrounding implementation and its callers in src/term.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "ascii_width"
dir = "05_terminal"
test = true
strict_clippy = true
hint = "1. Contract: Write only ASCII bytes that fit the remaining terminal width and account for the bytes actually written.\n2. Read the surrounding implementation and its callers in src/term.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "unicode_width"
dir = "05_terminal"
test = true
strict_clippy = true
hint = "1. Contract: Limit by Unicode scalar values rather than slicing inside a UTF-8 character. Update the same output-width budget.\n2. Read the surrounding implementation and its callers in src/term.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "progress_counter"
dir = "05_terminal"
test = true
strict_clippy = true
hint = "1. Contract: Increment the completed check count and redraw the counter.\n2. Read the surrounding implementation and its callers in src/term.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "progress_bar"
dir = "05_terminal"
test = true
strict_clippy = true
hint = "1. Contract: Restore a progress-bar stage while retaining the original width calculations and completed/pending formatting.\n2. Read the surrounding implementation and its callers in src/term.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "canonical_paths"
dir = "05_terminal"
test = true
strict_clippy = true
hint = "1. Contract: Resolve a path to a Unicode canonical path; retain the existing Windows-prefix behavior and return None for failures.\n2. Read the surrounding implementation and its callers in src/term.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "file_link_frame"
dir = "05_terminal"
test = true
strict_clippy = true
hint = "1. Contract: Style a file path using the caller-provided writer and restore terminal attributes afterward.\n2. Read the surrounding implementation and its callers in src/term.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "file_hyperlink"
dir = "05_terminal"
test = true
strict_clippy = true
hint = "1. Contract: Emit the original terminal hyperlink sequence around the displayed file path.\n2. Read the surrounding implementation and its callers in src/term.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "ansi_buffer"
dir = "05_terminal"
test = true
strict_clippy = true
hint = "1. Contract: Append an ANSI command to a byte buffer through the original formatting adapter.\n2. Read the surrounding implementation and its callers in src/term.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "scroll_initial"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Initialize scroll state without subtracting below zero when the selected row is close to the start.\n2. Read the surrounding implementation and its callers in src/list/scroll_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "scroll_window"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Keep the selection visible with padding, then clamp the viewport against the total row count.\n2. Read the surrounding implementation and its callers in src/list/scroll_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "scroll_next"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Move one row forward when a selection exists and clamp at the final row.\n2. Read the surrounding implementation and its callers in src/list/scroll_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "scroll_previous"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Move one row backward without underflow; an absent selection remains absent.\n2. Read the surrounding implementation and its callers in src/list/scroll_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "scroll_resize"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Clear selection for an empty list and clamp a previous selection after rows are removed.\n2. Read the surrounding implementation and its callers in src/list/scroll_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "scroll_padding"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Limit padding to one quarter of the visible rows and the configured maximum.\n2. Read the surrounding implementation and its callers in src/list/scroll_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "scroll_viewport"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Update the viewport size, recompute padding, and then make the selected row visible.\n2. Read the surrounding implementation and its callers in src/list/scroll_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "list_model"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Construct list state from application progress and the terminal dimensions, preserving row-index mappings.\n2. Read the surrounding implementation and its callers in src/list/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "list_resize"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Update the rendering dimensions and the scroll viewport for the available rows.\n2. Read the surrounding implementation and its callers in src/list/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "list_rows"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Restore one stage of row rendering: selection, completion status, file names, and clipping must remain synchronized.\n2. Read the surrounding implementation and its callers in src/list/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "list_render"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Restore one stage of the existing list screen; retain search input, filters, help keys, and terminal-size handling.\n2. Read the surrounding implementation and its callers in src/list/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "list_filter"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Rebuild visible exercise indices from the active completion filter and optional search filter.\n2. Read the surrounding implementation and its callers in src/list/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "list_index_mapping"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Translate a visible selection through filtered row indices, and report missing/out-of-range selections.\n2. Read the surrounding implementation and its callers in src/list/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "list_reset"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Reset the selected exercise and update the corresponding filtered row state.\n2. Read the surrounding implementation and its callers in src/list/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "list_search"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Apply the original substring search to exercise names and synchronize the displayed rows and selection.\n2. Read the surrounding implementation and its callers in src/list/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "list_open"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Resolve a selected visible row, make its exercise current, and indicate whether the list should exit.\n2. Read the surrounding implementation and its callers in src/list/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "list_keys"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Restore one input-handling branch while preserving filtering, navigation, search, reset, and quit behavior.\n2. Read the surrounding implementation and its callers in src/list.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "list_lifecycle"
dir = "06_navigation"
test = true
strict_clippy = true
hint = "1. Contract: Enter the original list terminal mode and restore it when handling finishes, including error paths.\n2. Read the surrounding implementation and its callers in src/list.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "input_pause"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Pause watcher-triggered work while an exercise is running; the existing Drop implementation resumes it.\n2. Read the surrounding implementation and its callers in src/watch.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "debounce_updates"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Restore the debouncer stage so bursts of file events produce one update per modified exercise per quiet interval.\n2. Read the surrounding implementation and its callers in src/watch/notify_event.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "file_notifications"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Ignore unrelated events and non-Rust files, then map changed Rust filenames to exercise indices.\n2. Read the surrounding implementation and its callers in src/watch/notify_event.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "terminal_events"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Translate terminal events to watch events while respecting pause/resume coordination and manual-run mode.\n2. Read the surrounding implementation and its callers in src/watch/terminal_event.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_model"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Create the watch state and terminal-event thread using the original channel types and initial pending status.\n2. Read the surrounding implementation and its callers in src/watch/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_run"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Run the selected exercise, record its output and done status, coordinate the editor, and render the result.\n2. Read the surrounding implementation and its callers in src/watch/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_reset"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Restore a reset-confirmation stage and resume the event thread when the interaction completes.\n2. Read the surrounding implementation and its callers in src/watch/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_change"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Only rerun for a change to the current exercise. Preserve the index comparison and output propagation.\n2. Read the surrounding implementation and its callers in src/watch/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_completion"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Both done variants permit advancing; Pending does not.\n2. Read the surrounding implementation and its callers in src/watch/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_advance"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Advance through AppState only when the current exercise is complete; otherwise report CurrentPending.\n2. Read the surrounding implementation and its callers in src/watch/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_prompt"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Restore a prompt stage so only currently valid actions are advertised.\n2. Read the surrounding implementation and its callers in src/watch/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_render"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Restore one stage of the existing result screen: diagnostics, hints, solution link, progress, and current path.\n2. Read the surrounding implementation and its callers in src/watch/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_hint"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Reveal a hint once and redraw only when its visibility changes.\n2. Read the surrounding implementation and its callers in src/watch/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_check_all"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Run all checks while input is paused; select a newly pending exercise or display the final message.\n2. Read the surrounding implementation and its callers in src/watch/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_resize"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Redraw after an actual terminal-width change and avoid redundant redraws.\n2. Read the surrounding implementation and its callers in src/watch/state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_dispatch"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Restore one branch of the existing watch-event loop without changing event ownership or exit semantics.\n2. Read the surrounding implementation and its callers in src/watch.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_list_transition"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Switch between watch and list modes using their existing exit states.\n2. Read the surrounding implementation and its callers in src/watch.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "watch_start"
dir = "07_watch"
test = true
strict_clippy = true
hint = "1. Contract: Construct the watcher and channels and enter the existing watch/list loop with its original terminal cleanup.\n2. Read the surrounding implementation and its callers in src/watch.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "editor_process"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Execute an editor command and preserve output/error context without silently accepting nonzero exit status.\n2. Read the surrounding implementation and its callers in src/editor.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "editor_discovery"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Use the existing version probe to detect a program; process spawning failures mean unavailable.\n2. Read the surrounding implementation and its callers in src/editor.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "editor_selection"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Choose the configured editor, VS Code, or Zellij according to the original precedence and argument parsing.\n2. Read the surrounding implementation and its callers in src/editor.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "editor_launch"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Restore one launch stage, preserving asynchronous startup and the editor-specific exercise path arguments.\n2. Read the surrounding implementation and its callers in src/editor.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "editor_close"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Close managed Zellij panes while retaining the original behavior for ordinary command-based editors.\n2. Read the surrounding implementation and its callers in src/editor.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "editor_join"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Join an optional editor startup task and propagate its returned editor or error.\n2. Read the surrounding implementation and its callers in src/editor.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "pane_identifier"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Parse the existing terminal_<decimal-id> newline format and return both the original digits and their numeric value.\n2. Read the surrounding implementation and its callers in src/editor/zellij.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "pane_status"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Parse Zellij pane JSON and determine whether the requested pane ID is still present.\n2. Read the surrounding implementation and its callers in src/editor/zellij.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "pane_close"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Pass the pane ID as a separate argument to the original Zellij close-pane command.\n2. Read the surrounding implementation and its callers in src/editor/zellij.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "app_editor_start"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Coordinate the application current-exercise path with the optional editor startup task.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "app_editor_join"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Join the startup task and put the resulting editor back into AppState.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "app_editor_close"
dir = "08_editors"
test = true
strict_clippy = true
hint = "1. Contract: Close the optional editor and preserve the no-editor path.\n2. Read the surrounding implementation and its callers in src/app_state.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_names"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Permit alphanumeric characters and underscores in exercise names. Report the first forbidden character.\n2. Read the surrounding implementation and its callers in src/dev/check.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a BUG mission. Compare each changed value, index, argument, or branch against the contract. Use the failing regression output to narrow the branch.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_manifest"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Generate the expected binary registration and compare it against the manifest without modifying that manifest.\n2. Read the surrounding implementation and its callers in src/dev/check.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_catalog"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Restore one validation stage for unique names, hints, paths, TODO guidance, and declared test presence.\n2. Read the surrounding implementation and its callers in src/dev/check.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_file_inventory"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Reject undeclared files and excessive directory nesting while allowing chapter READMEs and declared inputs.\n2. Read the surrounding implementation and its callers in src/dev/check.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_unsolved"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Run every non-exempt starter and reject an already solved exercise; propagate worker failures.\n2. Read the surrounding implementation and its callers in src/dev/check.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_format"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Validate the format version and combine metadata, directory, and unsolved-starter checks.\n2. Read the surrounding implementation and its callers in src/dev/check.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_solutions"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Restore one solution-validation stage, preserving required-solution handling, strict Clippy, formatting, and unexpected-file checks.\n2. Read the surrounding implementation and its callers in src/dev/check.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_gate"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Enforce catalog-size and manifest constraints before validating starters and solutions.\n2. Read the surrounding implementation and its callers in src/dev/check.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_directories"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Create a relative project directory and attach its path to any failure.\n2. Read the surrounding implementation and its callers in src/dev/new.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_files"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Write generated project content to the named relative file and retain actionable error context.\n2. Read the surrounding implementation and its callers in src/dev/new.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_new_course"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Restore one community-course initialization stage, including the original optional Git behavior.\n2. Read the surrounding implementation and its callers in src/dev/new.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_update_manifest"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Regenerate only the bin list and preserve the rest of the existing manifest.\n2. Read the surrounding implementation and its callers in src/dev/update.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_update"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Update the proper manifest for official development or a community course without redesigning either workflow.\n2. Read the surrounding implementation and its callers in src/dev/update.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "author_dispatch"
dir = "09_authoring"
test = true
strict_clippy = true
hint = "1. Contract: Dispatch New, Check, and Update through their original handlers and preserve the debug-build restriction.\n2. Read the surrounding implementation and its callers in src/dev.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "embedded_macro"
dir = "10_launch"
test = true
strict_clippy = true
hint = "1. Contract: Restore a stage of the real procedural macro: ordered exercises, solutions, directory indices, inputs, and READMEs must remain aligned.\n2. Read the surrounding implementation and its callers in rustlings-macros/src/lib.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."

[[exercises]]
name = "launch_dispatch"
dir = "10_launch"
test = true
strict_clippy = true
hint = "1. Contract: Restore the missing launch stage, preserving priority commands, initialization checks, normal CLI dispatch, and exit status. Then reconstruct and run the complete checker.\n2. Read the surrounding implementation and its callers in src/main.rs. Follow inputs, state changes, errors, and the returned value separately.\n3. This is a TODO mission. Replace the todo! step with real code. Values used below the gap tell you which bindings or state changes that step must produce.\nThe canonical solution is revealed after completion. The source map and architecture record are in exercises/README.md."
